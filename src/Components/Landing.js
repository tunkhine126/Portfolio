import React from 'react';

const Landing = () => {
  // const imageUrl = window.innerWidth >= 650 ? desktopImage : mobileImage;

  return (
    <div className="bg">
        <h1>Hello World, I'm Tun Khine</h1>
        {/* <p>They are good</p> */} 
      </div>
  );
};

export default Landing;